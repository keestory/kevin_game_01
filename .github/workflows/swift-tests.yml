name: Swift Tests

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  game-core:
    runs-on: macos-15
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Run deterministic game-rule tests
        run: swift test
